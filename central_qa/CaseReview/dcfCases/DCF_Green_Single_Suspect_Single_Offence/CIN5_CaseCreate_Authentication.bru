meta {
  name: CIN5_CaseCreate_Authentication
  type: http
  seq: 1
}

post {
  url: {{CaseCreate_AuthUrl}}/api/authenticate
  body: formUrlEncoded
  auth: inherit
}

headers {
  accept: application/json
  x-functions-key: {{CaseCreate_XfunKey}}
}

body:form-urlencoded {
  username: {{DCF_Username}}
  password: {{DCF_Password}}
}

script:post-response {
  const response = res.getBody();
   
  bru.setEnvVar("CaseCreateDcfAuthValue", JSON.stringify(response));
   
  console.log("environment variables set:", response);
  
  if (response.Token) {
      bru.setGlobalEnvVar("CaseCreateDcfAuthToken", response.Token);
      console.log("CaseCreateDcfAuthToken:", response.Token);
  } else {
      console.log("No Token");
  }
  
  
}

settings {
  encodeUrl: true
  timeout: 0
}
