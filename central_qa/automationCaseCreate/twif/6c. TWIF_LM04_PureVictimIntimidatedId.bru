meta {
  name: 6c. TWIF_LM04_PureVictimIntimidatedId
  type: http
  seq: 10
}

post {
  url: {{CaseCreate_Url}}/api/twif/lm04
  body: json
  auth: inherit
}

headers {
  X-Auth-Cookie: {{CaseCreateTwifAuthValue}}
  User-Agent: Mozilla/4.0 (compatible; MSIE 7.0; Windows NT 10.0; WOW64; Trident/7.0; .NET4.0C; .NET4.0E; .NET CLR 2.0.50727; .NET CLR 3.0.30729; .NET CLR 3.5.30729; InfoPath.3)
}

body:json {
  {
    "NewWitnessOrVictim": {
      "ItemId": "{{Twiflm04PureVictimIntItemId}}",
      "PTIURN": {
        "Force": "{{TwifForce}}",
        "Unit": "{{TwifUnit}}",
        "Number": "{{TwifPTIURNNumber}}",
        "Year": "{{TwifYear}}"
      },
      "Person": {
        "UniqueId": "{{TwifPureVictimIntId}}",
        "Name": {
          "GivenName": "{{$randomLastName}}",
          "FamilyName": "{{$randomFirstName}}"
        },
        "ContactDetails": {
          "Address": {
            "AddressLine": [
              "{{$randomStreetName}}",
              "{{$randomCity}}"
            ],
            "Postcode": "NE1 1BB"
          },
          "ContactNumber": [
            {
              "Number": {
                "TelNationalNumber": "07900998844"
              },
              "TypeOfNumber": "mobile"
            },
            {
              "Number": {
                "TelNationalNumber": "01912587414"
              },
              "TypeOfNumber": "home"
            }
          ],
          "Email": "vic_test@email.co.gov.uk"
        },
        "PersonClassification": [
          {
            "ClassificationType": "TypeOfWitness",
            "ClassificationCode": "Intimidated",
            "ClassificationValue": {
              "ClassificationValueFlag": true
            }
          }
        ],
        "TypeOfPerson": "Victim",
        "DateOfBirth": "1970-08-03",
        "Victim": {
          "VPSStatus": {
            "ResponseToVPSOffer": "Accepted",
            "SchemeExplained": "false"
          }
        }
      }
    }
  }
}

script:pre-request {
  function generateFiveDigitNumber() {
      return Math.floor(10000 + Math.random() * 90000);
  }
  
  function generateSevenDigitNumber() {
      return Math.floor(1000000 + Math.random() * 9000000);
  }
  
  function generateElevenDigitNumber() {
      return Math.floor(10000000000 + Math.random() * 90000000000);
  }
  
  function generateUppercaseAlphaNumeric(length) {
      const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789';
      let result = '';
      for (let i = 0; i < length; i++) {
          result += chars.charAt(Math.floor(Math.random() * chars.length));
      }
      return result;
  }
  
  let twiflm04PureVictimIntItemId = generateUppercaseAlphaNumeric(12);
  let twifPureVictimIntId = generateUppercaseAlphaNumeric(12);
  
  bru.setGlobalEnvVar("Twiflm04PureVictimIntItemId", twiflm04PureVictimIntItemId);
  bru.setGlobalEnvVar("TwifPureVictimIntId",twifPureVictimIntId );
  
  
  
}

script:post-response {
  const response = res.getBody();
  
  bru.setGlobalEnvVar("TwifPureVictimIntCreateReqId",response.requestId);
}

settings {
  encodeUrl: true
  timeout: 0
}
