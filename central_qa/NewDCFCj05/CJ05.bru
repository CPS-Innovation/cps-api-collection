meta {
  name: CJ05
  type: http
  seq: 2
}

post {
  url: {{CIN5_baseUrl}}/inbound-json
  body: json
  auth: inherit
}

body:json {
  {
    "CaseStatus": {
      "InformationSubmitted": {
        "Reason": "Provide Charging Decision",
        "Certification": null,
        "InformationProvided": null,
        "FactualSummary": null,
        "CITP": null
      },
      "StatusChanged": null,
      "CaseContacts": null,
      "ItemId": "{{dcfCJ05ItemId}}",
      "PTIURN": {
        "Force": "45",
        "Unit": "MG",
        "Number": "{{DCFPtiUrnNumber}}",
        "Year": "26",
        "Split": null
      },
      "CaseId": "{{DCFCaseID}}"
    }
  }
}

script:pre-request {
  function generateFiveDigitNumber() {
      return Math.floor(10000 + Math.random() * 90000);
  }
  
  function generateSevenDigitNumber() {
      return Math.floor(1000000 + Math.random() * 9000000);
  }
  
  function generateUppercaseAlphaNumeric(length) {
      const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789';
      let result = '';
      for (let i = 0; i < length; i++) {
          result += chars.charAt(Math.floor(Math.random() * chars.length));
      }
      return result;
  }
  
  
  let cj05 = generateUppercaseAlphaNumeric(12);
  
  bru.setGlobalEnvVar("dcfCJ05ItemId", cj05);
  
  console.log("dcfCJ05ItemId:", cj05);
  
  
  
}

settings {
  encodeUrl: true
  timeout: 0
}
