meta {
  name: PCDReviw-NotStated
  type: http
  seq: 14
}

get {
  url: {{StagingWmUrl}}/api/cases/{{TwifCaseId}}/pcd-review
  body: text
  auth: inherit
}

headers {
  Cms-Auth-Values: {{StagingTwifAuthValue}}
  x-functions-key: {{StagingWmKey}}
  User-Agent: Mozilla/4.0 (compatible; MSIE 7.0; Windows NT 10.0; WOW64; Trident/7.0; .NET4.0C; .NET4.0E; .NET CLR 2.0.50727; .NET CLR 3.0.30729; .NET CLR 3.5.30729; InfoPath.3)
}

body:text {
  
  
  
  
}

script:post-response {
  let responseData = res.getBody();
  
  if (responseData.status) {
      bru.setGlobalEnvVar("PcdRinitialStatus", responseData.status);
      console.log("PCD Review initial status", responseData.status);
  } else {
      console.log("PCD Review initial status not found");
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
